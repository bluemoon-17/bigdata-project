# 📊 LLM 기반 웹 보안 로그 분류 실험 결과 보고서

본 실험은 **CSIC 2010 웹 보안 데이터셋**을 활용하여 HTTP 요청(Request)의 정상(`Normal`) 및 이상(`Anomalous`) 여부를 로컬 LLM을 통해 이진 분류한 결과입니다. 프롬프트 엔지니어링 기법에 따른 정확도(Accuracy) 차이를 비교 분석했습니다.

---

## 1. 실험 결과 요약

| 프롬프트 기법 | 주요 특징 | 정확도 (Accuracy) | 성능 평가 |
| :--- | :--- | :---: | :--- |
| **대안 1: CoT (사고의 사슬)** | 단계별 추론 과정 및 다국어 예외 처리 가이드 포함 | **71%** | 🏆 **우수** (소형 모델의 맥락 이해도 극대화) |
| **대안 3: 단순 바이너리 지시** | Few-shot 없이 엄격하고 단순한 정의만 제공 | **58%** | ❌ **미흡** (정상 특수문자를 공격으로 오탐) |

---

## 2. 사용된 프롬프트 템플릿 코드

### 🔹 [Candidate 1] 사고의 사슬(Chain of Thought) 유도 템플릿 (정확도: 71%)
모델에게 분류 전 단계별 보안 체크리스트를 마음속으로 거치게 하여, 복잡한 정상 입력값(스페인어 특수문자, 긴 숫자 등)에 낚이지 않도록 유도한 프롬프트입니다.

```python
PROMPT_TEMPLATE = '''You are a strict Web Application Firewall (WAF) expert. 
Your job is to scrutinize the given HTTP request for critical web vulnerabilities.

Follow this step-by-step thinking process inside your mind before answering:
1. Decode any URL-encoded characters (like %20, %27, %3C) in the request.
2. Check if there are explicit malicious payloads: SQLi tokens, XSS tags, Path traversal dots, or malicious system commands.
3. Distinguish between long/complex benign inputs (such as foreign language registration forms, emails, credit card strings) and actual exploit attempts.

[Examples]
Request: GET /index.jsp?user_id=123 HTTP/1.1
Output: {{
  "analysis": "The request contains a standard page path and a simple numeric parameter. No malicious patterns or attack payloads are present.",
  "label": "Normal"
}}

Request: GET /search?q=%27%20OR%20%271%27=%271 HTTP/1.1
Output: {{
  "analysis": "The parameter 'q' contains a URL-encoded string '%27%20OR%20%271%27=%271' which decodes to \"' OR '1'='1\". This is a classic SQL Injection tautology attack designed to bypass authentication.",
  "label": "Anomalous"
}}

Request: GET /tienda1/miembros/editar.jsp?modo=registro&login=vilhelmi&password=elÁSTIcA&nombre=Theo&email=volkmann@siglo.be&direccion=Glorieta+Puerta+De+Hierro HTTP/1.1
Output: {{
  "analysis": "This is a user profile update request. Although it contains Spanish accents (Á) and special formatting (+ for spaces), all parameters represent legitimate user input data. No exploit code or suspicious keywords found.",
  "label": "Normal"
}}

[Instructions]
- You MUST respond ONLY with a valid JSON containing "analysis" and "label".
- Keep the "analysis" brief (1-2 sentences) detailing your security review.

Now classify:
Request: {http_text}
Output:'''





-----------------------------------------------------------------------------------------------------------------------------------------------

PROMPT_TEMPLATE = '''Classify this HTTP request as either "Normal" or "Anomalous".

- "Anomalous": The request contains an explicit web application exploit code (SQL Injection, XSS, Path Traversal).
- "Normal": The request is a safe, standard user action (including forms with foreign language letters, long numbers, emails, or signs like '+').

You must output exactly in this JSON format:
{{
  "label": "Normal" or "Anomalous"
}}

Request: {http_text}
Output:'''
